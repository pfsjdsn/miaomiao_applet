<view class="index">
  <swiper class="index_swiper" indicator-dots="{{indicatorDots}}" autoplay="{{autoplay}}">
    <block wx:for="{{imgList}}" wx:key="index">
      <swiper-item>
        <image src="{{item}}"></image>
      </swiper-item>
    </block>
  </swiper>
</view>