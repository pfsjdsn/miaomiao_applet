<view class="user">
  <view class="user_info">
    <image src="{{userPhoto}}"></image>
    <text  wx:if="{{isLogin}}">欢迎您{{nickName}}</text>
    <button  wx:else open-type="getUserInfo" bindgetuserinfo="bindGetUserInfo" class="log_btn">微信登录</button>
  </view>
</view>